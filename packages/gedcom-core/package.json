{
  "name": "gedcom-core",
  "version": "1.0.0",
  "description": "Language Server for GEDCOM validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "compile": "tsc -d -p .",
    "watch": "tsc -watch -d -p .",
    "test": "vitest"
  },
  "devDependencies": {
    "ts-brand": "^0.2.0",
    "vitest": "^3.2.4"
  }
}
